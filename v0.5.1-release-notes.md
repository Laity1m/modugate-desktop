# ModuGate v0.5.1

**发布时间：2026-08-06（建议按当前发布时间更新）**

## 修复与更新

- 修复了 `CLIProxyAPI` 运行组件在部分环境下缺失导致的启动失败问题（`CLIProxyAPI 运行组件不完整`）。
- 打包前会先执行 `ensure:cliproxy-runtime`，若 `runtime/cliproxy` 目录缺少可执行文件会自动补齐。
- 兼容 `cli-proxy-api.exe` 与 `CLIProxyAPI.exe` 两种大小写/命名差异，并会在运行时递归查找可执行文件。
- `release-windows` 工作流已改为按输入 tag 组装产物路径，避免上传固定 `0.5.0` 文件名。

## 下载

- `ModuGate-Setup-0.5.1-x64.exe`（Windows x64）
*** End Patch
