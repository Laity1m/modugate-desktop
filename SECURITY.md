# 安全说明

## 报告安全问题

请不要在公开 Issue 中粘贴账号密码、API Key、OAuth 授权文件、完整请求内容或其他敏感信息。

如果发现可能影响其他用户的安全漏洞，请通过 GitHub 仓库的 **Security → Advisories → Report a vulnerability** 私密报告，并提供：

- 受影响的 ModuGate 版本；
- 复现步骤和预期行为；
- 已去除凭据的日志或截图；
- 可能的影响范围。

## 安全边界

- ModuGate 是第三方本地工具，不提供或出售任何模型账号。
- 网页订阅不等于官方 API 额度，兼容转接不会改变上游服务条款。
- 本地服务默认只监听 `127.0.0.1`。轻量引擎的局域网共享必须手动开启，且只应在可信的专用网络中使用。
- 局域网共享不会开放管理接口、PostgreSQL 或 Redis；不要做路由器端口映射，也不要把未加密的 HTTP 接口暴露到公网。
- 二维码不包含 API Key。API Key 仍应视为密码，只提供给你信任的设备。
- 不要将 ModuGate 的应用数据目录、授权文件或密钥提交到代码仓库。
- 生产环境和敏感业务建议使用模型厂商的官方 API。
- Agnes API Key 与其他 API Key 一样使用 Windows 安全存储加密；中转不会绕过 Agnes 的计费、额度、审核或风控。
