# ModuGate v0.5.2

发布日期：2026-08-06

## 主要更新
- 新增「视频网关独立入口」：默认启动第二条网关在 `8788` 端口，对外可独立使用。
- 新增「视频网关独立 Key」：`视频网关 API Key` 与主网关 `统一 API Key` 分离，不再共用。
- 修复 `CLIProxyAPI` 启动组件重构导致的网关状态与服务链路异常，恢复独立网关状态展示与重启逻辑。
- 更新连接页展示与示例：新增视频网关 URL/Key 复制与编辑字段（端口/网关 Key 可独立保存）。

## 下载
- `ModuGate-Setup-0.5.2-x64.exe`
